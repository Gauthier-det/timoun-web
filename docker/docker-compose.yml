services:
  postgres:
    image: postgres:16-alpine
    container_name: timoun_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-strapi}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD est obligatoire}
      POSTGRES_DB: ${POSTGRES_DB:-timoun_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    # Pas de mapping de port : Postgres n'est accessible que via le reseau Docker interne
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-strapi} -d ${POSTGRES_DB:-timoun_db}"]
      interval: 10s
      timeout: 5s
      retries: 5

  back:
    build:
      context: ..
      dockerfile: docker/Dockerfile.back
    container_name: timoun_back
    restart: unless-stopped
    env_file: .env
    environment:
      HOST: 0.0.0.0
      PORT: 1337
      DATABASE_CLIENT: postgres
      DATABASE_HOST: postgres
      DATABASE_PORT: 5432
      DATABASE_NAME: ${POSTGRES_DB:-timoun_db}
      DATABASE_USERNAME: ${POSTGRES_USER:-strapi}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD est obligatoire}
      NODE_ENV: production
    # En prod derriere un reverse-proxy, retirer ce mapping et router via Nginx
    ports:
      - "1337:1337"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - strapi_uploads:/app/public/uploads

  front:
    build:
      context: ..
      dockerfile: docker/Dockerfile.front
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:1337}
    container_name: timoun_front
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - back

volumes:
  postgres_data:
  strapi_uploads: